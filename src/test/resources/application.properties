# No background timers during tests.
#
# The relay and the metrics refresh both run on schedules in production. Left
# on here, they publish outbox rows and recompute gauges in the middle of
# assertions, and the suite becomes flaky in a way that is very hard to read.
# Every test that cares about the relay calls it directly instead.
wallet.schedules.enabled=false
